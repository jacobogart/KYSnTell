import React, { Component } from 'react';
import { connect } from 'react-redux';

export class TellHome extends Component {
  render() {
    return (
      <div>
        
      </div>
    );
  }
}

export const mapStateToProps = (state) => ({

});

export default connect(mapStateToProps,)(TellHome);